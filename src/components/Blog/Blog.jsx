import React, {useState} from "react";

const Blog = (props) => {
    // const [blog, setBlog] = useState(Boolean(localStorage.getItem("blog")))

  return (
    <div>
      <h3>blog</h3>
    </div>
  );
};

export default Blog;
